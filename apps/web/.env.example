# Keep this unset on Horizons so the default /hcgi/platform proxy is used.
# For independent hosting, set it to your PocketBase base URL, for example:
# VITE_POCKETBASE_URL=https://api.example.com
VITE_POCKETBASE_URL=
